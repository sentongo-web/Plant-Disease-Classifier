# src/__init__.py
# This file marks the `src` directory as a Python package.
# Without it, Python would not recognise `src` as something you can import from.
# It stays empty on purpose — it is just a signal file.
